<h2>User info</h2>
<div>
  <%= image_tag @user.get_profile_image %>
  <h6><%= @user.name %></h6>
  <h6>introduction</h6>

  <%= link_to "アイコン", edit_user_path(current_user.id) %>
</div>

<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book form beign saved:</h2>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
        <% end %>
      </ul>
  </div>
<% end %>

<h2>New Book</h2>
<%= form_with model: @book, local:true do |f| %>
  <p>Title<p>
    <%= f.text_field :title %>

  <p>Opinion</p>
    <%= f.text_area :body %><br>

  <%= f.submit 'Create Book' %>

<% end %>